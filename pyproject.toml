[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "dungeon-stats-display_minescript"
version = "0.1.0"

[tool.setuptools.package-dir]
"" = "system/lib"
